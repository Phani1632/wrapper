pipeline {
    agent any

    tools {
        maven 'Maven 3.9.14'
        jdk 'JDK'
    }

    stages {

        stage('Build') {
            steps {
                sh 'mvn clean compile'
            }
        }

        stage('Package') {
            steps {
                sh 'mvn package'
            }
        }
    }
}